# ── Discord ────────────────────────────────────────────────────────────────
discord.py>=2.4.0
PyNaCl>=1.5.0          # requerido para voz

# ── Google AI (SDK activo — google-generativeai está deprecado) ───────────
google-genai>=1.0.0

# ── OpenAI SDK (usado por NimLLM / OpenRouterLLM / CustomLLM) ─────────────
openai>=1.40.0

# ── Base de datos ──────────────────────────────────────────────────────────
aiosqlite>=0.20.0
chromadb>=0.5.0     # Base de datos vectorial dedicada para búsqueda semántica

# ── Embeddings semánticos (EmbedEngine — all-MiniLM-L6-v2) ────────────────
sentence-transformers>=3.3.0
sentencepiece>=0.2.0     # Requerido por MarianTokenizer (curse_translator)
sacremoses>=0.1.0        # Pre/post-procesado para MarianMT (curse_translator)

# ── TTS ────────────────────────────────────────────────────────────────────
# Piper TTS se instala a nivel de sistema (binario externo).
# Ver install.sh para instrucciones de instalación.
# kokoro-onnx y onnxruntime eliminados — Fairy usa Piper, no Kokoro.

# ── Visión — procesado de vídeo ────────────────────────────────────────────
opencv-python-headless>=4.8.0

# ── Utilidades ─────────────────────────────────────────────────────────────
loguru>=0.7.2
python-dotenv>=1.0.0
psutil>=5.9.0            # Métricas CPU/RAM en api_server y tui
json-repair>=0.25.0      # Repara JSON malformado de respuestas LLM

# ── Generación de ilustraciones SVG/PNG/GIF ────────────────────────────────
cairosvg>=2.7.0          # SVG → PNG (estático). Sin soporte SMIL — ver svg_engine.py
Pillow>=10.0.0           # Ensamblado de GIF a partir de frames PNG

# ── MediaGuard — detección de media duplicada ────────────────────────────────
onnxruntime>=1.16.0      # Inferencia ONNX para MobileNetV3/EfficientNet (CPU ARM64)
aiohttp>=3.8.0           # Descarga asíncrona de imágenes/GIFs desde URLs
beautifulsoup4>=4.12.0   # Extracción de OpenGraph/image de páginas web
hnswlib>=0.8.0            # Índice vectorial HNSW para búsqueda aproximada de similitud coseno

# ── MouthWashLLM (filtro local con GGUF) ──────────────────────────────────
llama-cpp-python>=0.3.0  # Runtime llama.cpp para modelos GGUF locales

# ── Herramientas para regenerar modelos ONNX (solo scripts/) ──────────────
# Usados por scripts/download_mobilenet_onnx.py para exportar MobileNetV3 a ONNX.
torchvision>=0.17.0
onnx>=1.15.0

# ── Cogs Opcionales / Dependencias Extras ──────────────────────────────────────
playwright>=1.40.0       # Requerido por cogs.recompensas
mafic>=2.11.0            # Requerido por cogs.music (música con Lavalink)
smartcrop>=0.5.0         # Requerido por cogs.music (smart crop fallback para thumbnails)
goodfaith>=0.6.0         # Requerido por cogs.automod_v3

